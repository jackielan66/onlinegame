version: '3.8'

services:
  app:
    image: your-docker-username/online-game:latest
    container_name: online-game
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: always

